综合之前的部署，我决定使用spring进行业务端管理，python接受必要参数和任务id进行算法处理，使用kafka进行队列，celery进行多线程长时间计算任务处理，数据库使用mysql

我去，这idea突然更新得这么好看哈哈，搞得我有点不敢相信了，确实好用，速度流畅并且布局主题也好看了，对ai得集成也终于是跟上vscode了

晚上基本搞定了，kafka和spring后端的mysql、mybatis都搞定了，mybatis比以前好用不少，直接写主类还挺方便。
