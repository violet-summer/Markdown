终于搞定布局生成的解耦了，现在svg布局保存也正常了，接下来着重处理svg布局编辑到模型生成的测试情况。但是现在kafka的相关进度消息和结果消息的通信以及携带的数据库操作还没有测试完成，包括很多文件的保存字段也没有明确可靠传递。

最大的麻烦出现了，现在kafka一堆参数传递混乱至极，需要花不少时间同步了。

这个kafka消息定义现在主要从消费者端考虑需求，然后从生产者端进行定义，再回到消费者端逻辑进行优化。

踩坑python的celery方法只能传入基本的dict类型，不能使用自定义类型。

我底任务完成啦，我去，终于第一个生成接口从用户到布局id的控制全部搞定，包括svg和json全部正确