海洋obj缺少，并且好像修改没有对应到json内容进行修改，导致布局编辑效果没有正确影响到模型生成。出现了缺面问题。


还是不使用obj给前端渲染了，太麻烦了。


现在需要修改接口了，应该改为glb，请求不能直接携带string内容了

我需要你修改模型获取逻辑，我的模型glb文件存储在mino数据库的city桶之中的app目录下，相对路径依旧可从数据库之中获取，我希望你返回不是之前obj文件字符串而是预签名加密的url供web下载。

需要你补充pom配置以及mino配置，mino的ip是127.0.0.1:9000,key是admin，密钥是helloworld。


模型生成minio保存已经搞定，从java到python处理完成，就是现在测试web的端点了。


现在你获取模型文件不是直接解析obj_content的字符串了，而是通过后端发回的预签名url进行拉取下载和传输，然后渲染，minio://city/app/这是存储基本路径。

这是有所改动的接口  /api/models/model/{modelId}

其新的响应结构如下。

export interface Response { model?: ModelFileInfo; status?: number; [property: string]: any; } 
export interface ModelFileInfo { model_id?: string; presigned_url?: string; [property: string]: any; }