这个ai代码推进还是太不稳定了，并不能完全自主生成代码，优先确定项目结构和固定接口接口依旧重要，这次ai生成就是欠缺这个流程，导致效率不高，现在着手确定接口形状。

现在打算删除无用的文件项目，剪枝开始，现就这样整理到完成，算是第一阶段

1. 布局生成变为异步池子功能
2. 使用线程池进行服务和api接口解耦分离
3. 使用调用进度跟踪器（如progress_tracker.update_progress），将进度百分比、阶段描述等写入Redis（或数据库）。

这就是第一阶段完成吧。

自己的技术方案，python实现算法，不会作为web框架，也不会关心fastapi，所以task id加一个队列就不赖。