
# 网卡驱动

其一，切记避开第三方网站的通用设备驱动程序，因为文件不全且参杂大量虚假信息。

其二，目标机器是低版本/无网络的的机器的时候，自备一个干净的笔记本且内部是使用谷歌浏览器能够谷歌查询，然后使用u盘物理拷贝到目标机器即可。

# frp

## 最基本的状态

服务端（云服务器）下载frps并进行运行，配置成自动开机运行或者注册到etc\system下成为服务。

服务端云服务器要开放一个7000控制端口，以及提供7000端口进行数据通路复用，本地提供随机端口进行数据通信

客户端填写云服务器公网ip和需要配置远程公网端口和本地服务端口的映射


## 需要的命令操作