今日起床状态不错，上午就着手写一点论文，定下框架并进行两个章节的填充，最好在3000字左右。

### 个人爱好的开发

1. cpp构建模型生成的算法
2. 和斯坦福小镇的结合规划，因为目前不知道斯坦福小镇怎么使用，需要做一下探索
3. 然后web、java那一部分就可以不用修改了，就此结束不做任何修改了。
4. 新的项目使用本地数据库、队列，完全单机比较好一些，这样随时可以迁移。

### 个人发展

1. 开启关于ai学习的规划，然后趁早索要工位
2. 自己从零搓一搓简单的ai，因为目前对ai的黑盒机制确实一无所知
3. 新的ai也需要了解一下，比如glm开源模型

### 其它个人项目

1. 尝试一手cursor开发，付费不可避免了
2. 尝试规划俊源的项目，但是又得学习到rust了，很烦躁。
3. 维护一点个人的小项目了，目前这个多模块的程序还是不利于整合，就根本放不在一个项目之下，就很难受，说不定Tauri可以搞定，这样子就永远不用理会烦人的各个模块之间的通信问题了，省去一大堆阻碍迭代的次要开发量。
### 长期规划

#### 购物

1. 买一个运动相机录制vlog，并不在意实际的相机
2. 有奖学金就攒下等待换购笔记本或者固态硬盘或者手机
3. 买一双羽毛球鞋
4. 攒钱有活动余额，生活费要严格把控

### 论文待处理问题

1. 有太多代码存在，需要调整使用伪代码，因此空余出来的内容需要补充
2. 关于测试方面，目前仅仅只有演示，关于各个指标还有待测试

### 客户端程序整合提示词


处于快速迭代的需求，我需要避免对web相关服务所需系统分布部署的使用，而是构建前后一体的单机客户端应用，将原本存在的代码进行优化整合并优化后端逻辑，并且相关参考位于src\assets目录下。

#### 前端

1. 删除无用的用户管理，每一个客户端目前都是各自独立的
2. 但是原本的前端逻辑面板依旧保持不变

#### 后端

#### 阶段1

1. cpp算法改造自原本的python逻辑，但是模块（h/cpp）分层需要优化
2. 应用我目前的cmake配置，关于核心算法依旧使用cpp实现，包管理器依旧vcpkg
3. cpp实现尽量避免原本的继承/多态问题，使用20标准特性，常采用std::variant或者组合方式，并且关键算法尽量采用cpp已有的成熟库
4. 利用现代cpp的模块系统，并且拆分头文件和cpp文件
5. 剔除python部分的队列以及对象数据库，这些东西将会交给rust处理，仅仅重写关键的布局生成代码为现代cpp，最终目的是给rust调用供桌面应用的前端使用
6. rust充当胶水层，负责文件管理和内嵌数据库，以及充当front部分的后端并粘合cpp部分代码调用
7. 模型挤出的数据格式需要修改，字段要包括id字符串、名称、模型路径（相对）、模型尾缀（glb/obj）、以及模型类型（道路（main、major、minor）、建筑、海洋、河流、区块block、公园等）、还有额外的可选自定义属性。
#### 阶段2

1. 将此python代码改为cpp算法，模块（h/cpp）分层需要优化，原有cpp程序已经删除，你需要在现有python上直接进行修改
2. 参照cmake配置，关于核心算法依旧使用cpp实现，包管理器依旧vcpkg
3. cpp实现尽量避免原本的继承/多态问题，使用20标准特性，常采用std::variant或者组合方式，并且关键算法尽量采用cpp已有的成熟库
4. 利用现代cpp的模块系统，并且拆分头文件和cpp文件
5. 剔除python部分的队列以及对象数据库，这些东西将会交给rust处理，仅仅重写关键的布局生成代码为现代cpp，最终目的是给rust调用供桌面应用的前端使用
6. rust充当胶水层，负责文件管理和内嵌数据库，以及充当front部分的后端并粘合cpp部分代码调用，因此cpp需要额外提供c接口
7. 模型挤出的数据格式需要修改，字段要包括id字符串、名称、模型路径（相对）、模型尾缀（glb/obj）、以及模型类型（道路（main、major、minor）、建筑、海洋、河流、区块block、公园等）、还有额外的可选自定义属性。
8. 需要补充测试，测试结果输出在固定目录中，使用以下数据进行输入，测试正确后要留下文档
```
{

  world_dimensions: { x: 100, y: 100 },

  origin: { x: 0, y: 0 },

  zoom: 0.1,

  tensor_field: {

    smooth: true,

    set_recommended: true,

    grids: [

      { x: 10, y: 5, size: 16, decay: 2, theta: 59 },

      { x: 24, y: 12, size: 12, decay: 2, theta: 89 },

      { x: 24, y: 5, size: 12, decay: 2, theta: 28 },

    ],

    radials: [{ x: 50, y: 50, size: 15, decay: 0 }],

  },

  map: {

    animate: true,

    animate_speed: 10,

    water: {

      river_bank_size: 0.25,

      river_size: 0.5,

      coastline_width: 2,

      coastline: {

        noise_enabled: true,

        noise_size: 0.5,

        noise_angle: 20,

      },

      river: {

        noise_enabled: true,

        noise_size: 0.5,

        noise_angle: 20,

      },

      simplify_tolerance: 0.125,

      dev_params: {

        dsep: 0.25,

        dtest: 0.25,

        path_iterations: 400,

        seed_tries: 75,

        dstep: 0.25,

        dlookahead: 0.5,

        dcircle_join: 0.25,

        join_angle: 0.1,

      },

    },

    main: {

      dsep: 4,

      dtest: 0.5,

      dev_params: {

        path_iterations: 400,

        seed_tries: 5,

        dstep: 0.25,

        dlookahead: 6.25,

        dcircle_join: 0.25,

        join_angle: 0.1,

        simplify_tolerance: 0.0125,

        collide_early: 0,

      },

    },

    major: {

      dsep: 1.25,

      dtest: 0.5,

      dev_params: {

        path_iterations: 400,

        seed_tries: 5,

        dstep: 0.25,

        dlookahead: 2.5,

        dcircle_join: 0.25,

        join_angle: 0.1,

        simplify_tolerance: 0.0125,

        collide_early: 0,

      },

    },

    minor: {

      dsep: 1.25,

      dtest: 0.25,

      dev_params: {

        path_iterations: 400,

        seed_tries: 5,

        dstep: 0.25,

        dlookahead: 0.5,

        dcircle_join: 0.25,

        join_angle: 0.1,

        simplify_tolerance: 0.0125,

        collide_early: 0,

      },

    },

    parks: {

      cluster_big_parks: true,

      num_big_parks: 1,

      num_small_parks: 0,

    },

    buildings: {

      min_area: 5,

      max_length: 20,

      shrink_spacing: 2,

      chance_no_divide: 0.05,

    },

  },

  style: {

    colour_scheme: "Default",

    zoom_buildings: true,

    building_models: true,

    show_frame: true,

    orthographic: true,

    camera_x: 0,

    camera_y: 0,

  },

  options: {

    draw_center: true,

    high_dpi: true,

  },

  download: {

    image_scale: 1,

    type: "",

  },

  park_polygons: {

    max_length: 0.25,

    min_area: 1,

    shrink_spacing: 0.75,

    chance_no_divide: 1,

  },

};

```

#### 进度

终归是直接一步到位做不到，还是考虑将python原地改为cpp代码比较好

分开生成进度好一点，先各自运行完好，测一下app界面和cpp接口，留下文档今天就算任务完成

斯坦福小镇的兼容数据不知道需要什么


依旧头疼，ai的深度不够，可能转变思路，先逐个将python文件转为hpp实现，然后再进行所需要的优化。


#### 阶段3

1. 现在请你实现二维布局到三维模型的挤出cpp实现，使用网格算法鲁棒+可控：CGAL，复现原本python的逻辑，并清理无用代码
2. 布局生成的环形闭合存在问题，可能是末端join太严格了，请参照原本python进行微调、
3. 此外代码层次目前不够清晰，模块划分不合理，导致混乱，希望你重新规划，分离基础结构、程序中的输出存储结构以及算法结构以及接口结构处理等等，并将内部函数调用进行整理注释。
4. 关于流线存储以及网格存储以及张量场这种数据结构应该提取，因为其负责存储整个进程的全局数据比如张量、流线信息，其次像json_layout这种结构我觉得需要重构，因为其承载结果数据获取到实际svg文件的转换，代码结构还能优化。
5. 海岸多边形没有正确导出，断头路依旧非常多，导致不美观，且区块递归切分的概率太低了，很多区块都没有进行递归切分，或者说是递归切分只进行了一次

#### 阶段4

1. 要开始着手审视项目了，因为好像目前项目代码没有实现不同文件夹的管理哈哈
2. 此外进度条功能需要修改，改成任务队列吧，就像网盘的的下载任务队列一样。
3. 其实目前表单貌似太复杂了，也不知道怎么安排才合适
4. 空闲时间还是进行论文编写吧。
5. 博客任务还是和日常写作分开吧，现在这样很多东西我都不方便对外展示了。
6. 布局信息，配置成json文件，包括布局名称，布局生成时间，布局修改时间，布局生成所有的参数，保存到布局目录下
7. 任务队列像谷歌浏览器一样，有一个任务开始进度动画即可，除非点开查看，不用一直浮现那个进度状态，旧的进度显示也要删除
8. 模型的面生成依旧存在错误需要修改。