## 散乱记录



BST 和后续遍历都是用递归算法：

1. BST 插入使用递归算法，自己传入自己然后赋予自己新值 new 节点，最顶层返回一个 root 节点。
   1. 题目保证不重复则无需要处理相等的情况
   2. 释放内存，依旧递归，是先左后右最后真正 delete，最开始如果为空指针就返回为空直接。
2. 后续遍历返回
   1. 先左右边再根部
   2. 初有真值，控制输出首个元素-即最左边的节点前不会有空格，之后所有元素输出内容之前都有空格。