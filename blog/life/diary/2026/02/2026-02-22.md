今天好不容易把kafka打通了 ，虽然还是有很多报错，有点无奈，希望进行合理测试后成功吧。

记住坑了，以后不论项目，要尽量保证命名方式统一，不然很多默认行为会导致不美观或者错误，很多库都有约定成俗的命名风格，与外部交互也默认使用，比如java使用驼峰命名。

此外，目前这个布局生成到编辑到模型这个编辑阶段选择依旧有问题，我现在是计划跳过流线阶段，从生成多边形以后开始考虑编辑，然后进一步变为模型。但是代码目前，流线转多边形转模型存在巨量耦合，想要分离存在一定难度。

思路现在已经明确，独立出来一个结构类，负责管理布局到json的转换，以及最终布局svg文件的生成，当然其中部分功能比如修改json/svg文件职能交给java业务端，但是svg到obj的转换依旧交给python端

然后再构建一个类负责进行json=》json_layout=》obj模型转换的问题

今天已经把布局生成的json保存结构和布局到obj的逻辑成功结构，明天就要准备实际的交互测试成功了。