以后换成图片加载缩略图而不是svg

设置触控板的速率，希望调节快一些，调节到更多手势

并且option存在很多快捷键，option+q开启搜索

叉号退出不会杀掉后台，

关掉系统自动更新
关掉siri功能
辅助功能-指针控制-触控板选项-三指拖移-拖动窗口

程序坞启用隐藏和放大

修改放达默认打开位置

修改访达设置，显示扩展名，执行搜索为当前文件名

下载mos处理鼠标和丝滑问题


### Card 自身加了重复的 overflow-hidden

[第71行](file:///F:/CODE/GIThub/LayoutCraft/src/renderer/components/layout/WorkspaceLayout.tsx#L71) 的 `overflow-hidden` 与 Card 组件自带的 `overflow-hidden`（[card.tsx 第15行](file:///F:/CODE/GIThub/LayoutCraft/src/renderer/components/ui/card.tsx#L15)）重复，进一步确保 ring 被裁剪。

### DOM 层级链（根因）

Plain Text

```
<div bg-background>                          ← 纯白背景
  <ResizablePanelGroup overflow:hidden>      ← 库注入
    <ResizablePanel>
      <div overflow:auto>                    ← 库注入，裁剪 ring 直边
        <Card h-full rounded-xl ring-1>      ← ring 只在圆角处可见
```


自定义div即可


不要一生成轮廓后就默认四选中第一个轮廓，这样非常影响体验，此外轮廓操作工具列应该补充一个取消轮廓选中或者点选中的按钮以及后台功能