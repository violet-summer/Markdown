今天终于是把app桌面端的布局生成搞定了，还得是直接python方便啊，cpp改写实在是费事儿。

可拓展性问题，现在所有单位数据比如轮廓和模型资产都是确定固有的类型，比如道路、海洋、建筑这些固有的关键字，是面对业务的，以及blocks下细分单个block的层次关系，这样导致未来后端或者前端随时拓展新关系会存在需要到处修改的问题，我觉得有必要将这些内容进行模板换，变成一个键值对属性，层次关系也依赖json层次结构进行动态解析。

此外现在模型生成依赖于布局生成提供的特定数据结构类型，如果未来我提供泛洪提取轮廓进行模型生成，这样数据结构又要大变，也希望进行通用模板化统一。

比如轮廓生成的基本结构。


### 发展

我的注意力应该从开发转变到其它地方了，不要再纠结web开发的事情了，总结完毕之后就以后以tauri开发即可，不用老操心杂七杂八的配置问题了。

转向前沿ai论文为要紧事。
