新主机上面也搞定了，最近到24号之前图书馆也不开放，我也发现买的册子也只是一点算法提示，其实用处并不是很大，主要还是担心初试能不能过。

今晚看看毕业设计进度，先把代码敲一敲，也不说别得，今天就把kafka得使用弄明白然后隔离前后端。

还是先从后端开始，先着手搞定登录和验证功能，但测试貌似先要搞定前端才可以。先把前端代理修正过来。

目前这个python已经着手清理，但是暂时想不到怎么测试kafka对python代码的调用是否成功，只能试一下直接调用程序接口是否可用生成正确的响应或者文件比较好。

今日好歹是基本把kafka的相关配置搞定了，但是缺少测试，一旦明天搞定，项目就只剩前端就可以告退了。

web端两部分的逻辑就是发送生成请求之后不断从web查询数据库中状态信息，当查到完成之后再次发出新的请求获取实际的模型和svg文件内容进行渲染。

但不是说result可以合并到progress之中，其实进度信息对应的是两种生成任务共同的job表，result对应的是各自实际存储生成结果对象的数据库表操作，因此要分开好一些。

吃了一个大亏，有`try`一定要打印输出错误，不然就直接结束不报错，不利于`debug`

经过千辛万苦，终于把模型生成的结果逻辑在服务端的通信搞定了，数据库、java、python和kafka都搞定了没有错误。