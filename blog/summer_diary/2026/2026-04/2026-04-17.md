早上顺手捎过去毕业登记表。

现在发现二维和三维同现在一个界面功能太多了好像，能不能有一个容器做到二维三维切换这样的更加好一些。

但是得保证二维数据修改的时候及时更新三维模型，但是也不是每次都是重新生成从而浪费算力气；

左侧列表的每一个item是固定大小，但是语义不明确，显示出了冗长的ID和非常精确的创建时间，这些似乎都是没有必要的，这些功能和信息可以收纳在右键单击之后展开的功能列表之中，不需要直接呈现在面板中；其次列表中元素应该是从顶部往下累计叠加而不是之前的全从中间开始。


### 开发

1. 需要对模型生成的各种情况的处理进行分别说明和分别处理了，还得厘清ai生成的逻辑进行调控。
2. 基本功能貌似都搞定了，接下来就是模型关系的管理了，并且管理模型的信息功能，就是选中一个模型其上浮现一个信息面板，显示模型的信息，并且这个信息是可配置可输入的，暂时不知道怎么设计。
3. 也要检查之前模型生成接口以及轮廓提取接口，留好文档
4. 然后除了音乐可视化基本都搞定了，虽然信息面板不够自定义和美观

