今天检查了一下图形生成逻辑发现依旧内外环的染色逻辑有问题，并且之前只渲染了外环，现在搞定了，效果还不错。

最终折服了，还是手动标记属性对应的json字段吧，不然很多自动生成工具不会自动检测yml文件的配置进行蛇形明明法的变换。

区块生成没有问题，自己多疑了，但是道路宽度依旧有待考量配比，不然总是和区块重叠在一起。

依旧出现在json到结构类型的这一部分数据。

补充切分区块的json和svg测试成功，

模型生成文件已经正常了，但是不同区块的模型都合并在一块了，其实计划是每一块都分开生成的，然后数据库之类的都没有保存更新，状态更新的kafka其实还ok。


ok，这样单独生成模型的逻辑也搞定了，就是之前还有很多目录问题也理顺了，这个`path`在`join`的时候不能在新的路径上补充一个`/`，这样会导致跑到当前磁盘的根目录下面创造。


晚上九点，连带着模型生成进度的调试也打通了，可以正常写入数据库了。